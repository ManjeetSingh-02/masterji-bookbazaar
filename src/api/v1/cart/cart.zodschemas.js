// import external modules
import { z } from 'zod';

// zod schema for createCart
export const createCartSchema = z.object({});

// zod schema for updateCart
export const updateCartSchema = z.object({});
